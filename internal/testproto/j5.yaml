
packages:
  - name: test.v1

options:
  subPackages:
    - name: "service"

dependencies:
  - local: protostate

  - bufRegistry:
      owner: bufbuild
      name: protovalidate

  - bufRegistry:
      owner: googleapis
      name: googleapis

  - registry:
      owner: pentops
      name: j5

